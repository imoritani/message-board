<h1>メッセージ一覧</h1>
<% @messages.each do |msg| %>
  名前:<%= msg.name %>
  内容:<%= msg.body %>
  <br>
 <% end %>


<%= form_for(@message) do |f| %>
名前: 
 <%= f.text_field :name%>
内容:
 <%= f.text_field :body%>
 <%= f.submit%>
 <% end %>
